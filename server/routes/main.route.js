const express = require("express");
const router = express.Router();

// const controller = require("../controllers/main.controller");

// router.get("/ss", controller.main_get)

module.exports = router;